const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				lexend: ['Lexend Deca', 'sans-serif']
			}
		}
	},

	plugins: []
};

module.exports = config;
